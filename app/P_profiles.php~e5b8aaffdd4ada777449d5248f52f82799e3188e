<?php

namespace App;

use App\employee;
use Illuminate\Database\Eloquent\Model;

class P_profiles extends Model
{
    protected $table = 'profile_menu';

    protected $primaryKey = 'idP_profiles';
    protected $fillable   = [
        'name_profile',

    ];

    /** public function employee()
    {
    return $this->hasMany(employee::class);
    }   */
}
